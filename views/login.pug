doctype html
html
  head
    title Login
    link(rel="stylesheet", href="/css/styles.css")
  body
    .login-container
      img(src="/img/logo-lector-voraz.png" alt="El Lector Voraz Logo" style="width:240px; margin-bottom:24px;")            

      if error
        .error-message(style="color: #d32f2f; background: #ffebee; padding: 12px; border-radius: 6px; margin-bottom: 16px; border: 1px solid #ffcdd2;")
          p(style="margin: 0; font-weight: bold;") ❌ Usuario o contraseña incorrectos

      form(action="/login" method="POST")
        label(for="usuario") Usuario/Email:
        input(type="text" name="usuario" required value=usuario placeholder="Ingresa tu usuario o email")
        label(for="contrasena") Contraseña:
        input(type="password" name="contrasena" required placeholder="Ingresa tu contraseña")
        button(type="submit" class="button") Ingresar
